#include <stdio.h>

int main (){

    int fahr, cel;
    int start, stop, increment;
    start = 1;
    stop = 10;
    while (start <= stop){
        printf("%d", start);
        start = start + increment;

    }
    return 0;
}