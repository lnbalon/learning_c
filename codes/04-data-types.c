#include <stdio.h>
#include <stdlib.h>

int main() {

    int count = 0;
    double average = 1.2;
    char grade = 'C';

    printf("%d count, %f average, %c grade \n", count, average, grade);
    return 0;

}